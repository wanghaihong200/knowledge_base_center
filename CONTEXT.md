# 掌柜智库（知识库中心）

面向垂直领域设备手册/技术文档的企业级 RAG 智能知识库。两条流水线：把文档加工入库的**导入流水线**，和回答用户提问的**检索流水线**。

## Language

### 流水线与单元

**导入流水线 (Import Pipeline)**:
把 PDF/Markdown 文档解析、切片、向量化并写入向量库的完整处理链。
_Avoid_: 入库流程、ingestion

**检索流水线 (Query Pipeline)**:
接收用户提问，经过主体确认、多路检索、融合重排、生成答案的处理链。
_Avoid_: 查询流程、问答链

**切片 (Chunk)**:
文档按标题层级切分出的最小检索单元，是向量化与检索的基本粒度。
_Avoid_: 段落、分段、segment

**任务 (Task)**:
导入流水线中一个文件的一次处理实例，以 task_id 标识，供前端轮询节点级进度。
_Avoid_: 作业、job

**会话 (Session)**:
一次多轮问答的上下文单元，以 session_id 标识，消息历史持久化。
_Avoid_: 对话、conversation

**消息 (Message)**:
会话中的一条用户或助手发言，含角色、文本、关联主体与图片。

### 领域概念

**主体 (Item Name)**:
一份文档所描述的设备/产品的规范名称（如"H3C ER2100企业级路由器"），导入时由 LLM 识别，检索时用于确认与过滤。
_Avoid_: 商品名、产品名、实体、主体名称

**主体确认 (Item Name Confirm)**:
检索前从用户提问中提取主体、与库中已知主体对齐；无法确定时向用户反问的机制。
_Avoid_: 意图识别、产品确认

**假设性文档 (HyDE Doc)**:
由 LLM 根据用户查询生成的"专家回答范文"，以其向量做第二路检索，弥补问句与文档的表述差异。
_Avoid_: 虚拟文档、hyde 文档

**融合 (Fusion)**:
向量检索与 HyDE 检索两路结果按排名倒数加和合并排序的方法。
_Avoid_: RRF、结果合并

**断崖截断 (Cliff Cutoff)**:
重排后检测相邻相关分数的突变点，在其处截断低相关文档的策略。
_Avoid_: 阈值过滤、top-k 截断

**图片替换 (Image Rewrite)**:
导入时把 Markdown 内的本地图片上传对象存储，并用视觉模型生成的摘要替换图片描述文本。
_Avoid_: 图片处理、图片上传
